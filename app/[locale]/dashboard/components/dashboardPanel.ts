/**
 * Shared dashboard surface styles — use for cards and sections (terminal aesthetic).
 * Prefer `rounded-2xl` + subtle surface for consistency across /dashboard routes.
 */
export const dashboardPanelClassName =
  "rounded-2xl border border-[var(--border)] bg-[var(--surface)]/50";

export const dashboardPanelPaddingClassName = "p-4 md:p-6";
